# ZMK Persistent Default Layer (PDF) Behavior Module
# Provides persistent layer switching functionality

name: zmk-persistent-default-layer
build:
  cmake: .
  kconfig: Kconfig
  settings:
    board_root: .
    dts_root: .
    snippet_root: .
